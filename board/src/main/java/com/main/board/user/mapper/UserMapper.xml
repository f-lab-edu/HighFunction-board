<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.main.board.user.repository.UserRepository">

    <insert id="save" parameterType="com.main.board.user.DTO.UserDTO">
        INSERT INTO  user (user_id, password, name, create_date)
        VALUES (#{user_id}, #{password}, #{name}, #{create_date});
    </insert>

    <select id="findUserById" parameterType="com.main.board.user.DTO.UserDTO" resultType="boolean">
        SELECT EXISTS (SELECT *
                       FROM user
                       WHERE user_id = #{user_id}
        );
    </select>

</mapper>