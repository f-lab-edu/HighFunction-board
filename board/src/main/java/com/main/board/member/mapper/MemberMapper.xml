<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.main.board.member.repository.MemberRepository">

    <insert id="save" parameterType="com.main.board.member.Member">
        INSERT INTO member (member_id, password, name, create_date)
        VALUES (#{memberId}, #{password}, #{name}, #{createDate});
    </insert>

    <select id="findUserById" parameterType="com.main.board.member.DTO.SignupRequest" resultType="boolean">
        SELECT EXISTS (SELECT 1
                       FROM member
                       WHERE member_id = #{memberId}
        );
    </select>

</mapper>